services:
  database:
    container_name: postgres
    image: postgres:18
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: serv
      POSTGRES_USER: serv
      POSTGRES_PASSWORD: password

